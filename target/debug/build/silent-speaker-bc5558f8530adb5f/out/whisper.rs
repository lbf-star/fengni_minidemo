// This file is @generated by prost-build.
/// FEC帧：单个数据块或冗余块的封装
/// 注意：xxhash64字段计算时需排除自身（先设为0，计算后填充）
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct FecFrame {
    /// FEC会话标识：相同数据单元的k+m个块共享此ID
    ///
    /// 16字节UUID，用于块重组
    #[prost(bytes = "vec", tag = "1")]
    pub session_id: ::prost::alloc::vec::Vec<u8>,
    /// 块位置信息
    ///
    /// 范围：0 到 (k+m-1)
    #[prost(uint32, tag = "2")]
    pub block_index: u32,
    /// 原始数据块数（如：4）
    #[prost(uint32, tag = "3")]
    pub k: u32,
    /// 冗余块数（如：2）
    #[prost(uint32, tag = "4")]
    pub m: u32,
    /// 实际数据载荷：FEC编码后的数据块
    #[prost(bytes = "vec", tag = "5")]
    pub payload: ::prost::alloc::vec::Vec<u8>,
    /// 完整性校验：对整个FECFrame（除本字段外）计算xxHash64
    #[prost(fixed64, tag = "6")]
    pub xxhash64: u64,
    /// 块类型：原始数据块或冗余块
    #[prost(enumeration = "BlockType", tag = "7")]
    pub block_type: i32,
    /// 协议版本：便于未来升级
    #[prost(uint32, tag = "30")]
    pub version: u32,
}
/// FEC专用消息：仅包含FEC帧，无文本内容
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct FecWhisper {
    /// FEC数据帧
    #[prost(message, optional, tag = "1")]
    pub fec_frame: ::core::option::Option<FecFrame>,
}
/// Whisper消息：支持普通文本和FEC数据的统一容器
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct Whisper {
    /// 消息唯一标识：用于去重和追踪
    ///
    /// 16字节UUID，长度必须为16
    #[prost(bytes = "vec", tag = "1")]
    pub id: ::prost::alloc::vec::Vec<u8>,
    /// 时间戳：纳秒精度，固定长度编码最快
    #[prost(fixed64, tag = "3")]
    pub timestamp_ns: u64,
    /// 处理优先级：影响流调度和资源分配
    #[prost(enumeration = "Priority", tag = "4")]
    pub priority: i32,
    /// 消息负载：普通文本或FEC数据（二选一）
    #[prost(oneof = "whisper::Payload", tags = "6, 7")]
    pub payload: ::core::option::Option<whisper::Payload>,
}
/// Nested message and enum types in `Whisper`.
pub mod whisper {
    /// 消息负载：普通文本或FEC数据（二选一）
    #[derive(Clone, PartialEq, Eq, Hash, ::prost::Oneof)]
    pub enum Payload {
        /// UTF-8文本消息
        #[prost(string, tag = "6")]
        Content(::prost::alloc::string::String),
        /// FEC专用消息
        #[prost(message, tag = "7")]
        FecPayload(super::FecWhisper),
    }
}
/// 消息优先级：影响调度顺序和处理策略
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum Priority {
    /// 后台任务，可被抢占
    Low = 0,
    /// 普通数据，按顺序处理
    Normal = 1,
    /// 重要数据，优先处理
    High = 2,
    /// 关键信令，最高优先级
    Urgent = 3,
}
impl Priority {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Low => "PRIORITY_LOW",
            Self::Normal => "PRIORITY_NORMAL",
            Self::High => "PRIORITY_HIGH",
            Self::Urgent => "PRIORITY_URGENT",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "PRIORITY_LOW" => Some(Self::Low),
            "PRIORITY_NORMAL" => Some(Self::Normal),
            "PRIORITY_HIGH" => Some(Self::High),
            "PRIORITY_URGENT" => Some(Self::Urgent),
            _ => None,
        }
    }
}
/// FEC块类型：区分原始数据和冗余校验
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum BlockType {
    /// 原始数据块
    Original = 0,
    /// 冗余校验块
    Redundant = 1,
}
impl BlockType {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Original => "BLOCK_TYPE_ORIGINAL",
            Self::Redundant => "BLOCK_TYPE_REDUNDANT",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "BLOCK_TYPE_ORIGINAL" => Some(Self::Original),
            "BLOCK_TYPE_REDUNDANT" => Some(Self::Redundant),
            _ => None,
        }
    }
}
